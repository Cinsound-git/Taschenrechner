#include <stdio.h>
#include <math.h>

int main() {
    double zahle[100];
    int i = 0;
    double ergebnis = 0;

    while(t = f) {
        printf("Gebe die Zahle ein (Bei f eingabe wird die Schleife Bendet): \n");
        scanf("%lf",zahle[i]);
        i++;
        printf("Gebe die Zahle ein (Bei f eingabe wird die Schleife Bendet): \n");
        scanf("%lf",zahle[i]);


        if(operator = +) {
            ergebnis = zahle[i-1] + zahle[i];
        } else if(operator = -) {
            ergebnis = zahle[i-1] - zahle[i];
        } else if(operator = *) {
            ergebnis = zahle[i-1] * zahle[i];
        } else if(operator = /) {
            ergebnis = zahle[i-1] / zahle[i];
        }
        i++;
    }
    printf("Ergebnis ist: %lf",ergebnis);
}
